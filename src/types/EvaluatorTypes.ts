export type BoxEvaluatorType = {
    surface_area: number,
    volume: number,
    baseArea: number
}

export type BuildingMassEvaluatorType = {
    public_realm_area: number,
    volume: number,
    baseArea: number,
    total_building_area: number,
    lift_provision_area: number
}

export type LayoutEvaluatorType = {
}